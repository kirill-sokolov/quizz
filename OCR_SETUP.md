# OCR Setup Instructions

## Что добавлено

1. **Tesseract OCR** в Docker контейнер (английский и русский языки)
2. **OCR сервис** (`apps/api/src/services/ocr-service.ts`) для анализа изображений
3. **API endpoint** `/api/quizzes/:id/analyze-zip-ocr` для OCR анализа ZIP архивов
4. **UI кнопка** "Проанализировать ZIP с OCR" в админке (экспериментальная функция)

## Как использовать

1. Пересоберите Docker контейнер:
   ```bash
   docker-compose down
   docker-compose build
   docker-compose up -d
   ```

2. В админке на странице редактирования квиза:
   - Нажмите "🔬 Проанализировать ZIP с OCR"
   - Выберите ZIP архив с изображениями слайдов
   - Дождитесь результата (может занять 10-30 секунд)
   - Посмотрите результат в JSON viewer

## Результат OCR

OCR вернет JSON с распознанным текстом для каждого изображения:
```json
{
  "totalImages": 15,
  "results": [
    {
      "index": 0,
      "filename": "slide-01.png",
      "text": "Вопрос 1: Какой цвет?\nA. Красный\nB. Синий\nC. Зелёный\nD. Жёлтый"
    },
    ...
  ]
}
```

## Дальнейшие шаги

Если OCR подсказки окажутся полезными, можно:
1. Добавить передачу OCR hints в текстовую модель при парсинге DOCX
2. Использовать OCR для автоматической проверки качества распознавания вариантов ответа
3. Комбинировать OCR + vision модели для лучшего распознавания

## Технические детали

- Используется системный Tesseract через execSync
- Поддержка английского и русского языков (eng+rus)
- PSM режим 6 (uniform block of text) для лучшего распознавания слайдов
- Таймаут 30 секунд на изображение

# История выполненных задач

## 2026-02-25: Этапы 1-3 + Дополнительные улучшения

### ✅ Этап 1: Удаление OCR
**Цель:** Полностью убрать OCR из проекта (сестра делает более чёткий DOCX).

**Выполнено:**
- Удалён `ocr-service.ts`
- Удалён OCR endpoint `/api/quizzes/:id/analyze-zip-ocr`
- Удалён OCR UI из `QuizEdit.jsx` (states, handlers, buttons)
- Удалена функция `analyzeZipOcr` из `client.js`
- Удалён Tesseract из `Dockerfile`
- Убрано debug logging из TV компонентов

**Файлы:**
- `apps/api/src/services/ocr-service.ts` — DELETED
- `apps/api/src/routes/import.ts` — удалён импорт и endpoint
- `apps/web/src/pages/QuizEdit.jsx` — удалён OCR UI блок
- `apps/web/src/api/client.js` — удалена функция
- `apps/api/Dockerfile` — удалены tesseract зависимости

---

### ✅ Этап 2: Улучшение экрана регистрации
**Цель:** QR код больше, убрать цифры команд, улучшить дизайн.

**Выполнено:**
- QR код теперь адаптивный: `max-w-[600px]`, `aspect-square`, `max-h-[85vh]`
- Удалены номера команд `#{idx + 1}`
- Добавлены иконки команд (первая буква в круге)
- Улучшен дизайн: градиенты, тени, hover-эффекты, border-left

**Файлы:**
- `apps/web/src/components/TV/TVLobby.jsx`

---

### ✅ Этап 3: Улучшение экрана результатов
**Цель:** Увеличить шрифты в 2 раза, анимация появления мест (2→3→4→...→1).

**Выполнено:**
- **Увеличены шрифты:**
  - Заголовок: `text-6xl` → `text-9xl`
  - Строки команд: `text-3xl` → `text-6xl`
  - Медали/номера: `text-4xl` → `text-8xl`
  - Счёт: добавлен `text-5xl`
- **Изменён порядок появления:**
  - Реализована функция `getAnimationDelay(index)`
  - 2 место появляется первым (delay = 0s)
  - 3 место — через 0.15s
  - 1 место — последним (delay = (N-1) × 0.15s)
- Улучшена анимация: `opacity-0`, `animationFillMode: 'forwards'`

**Файлы:**
- `apps/web/src/components/TV/TVResults.jsx`

---

### ✅ Дополнительно: Конфигурация bot username через .env
**Цель:** Убрать хардкод `WeddingTest2Bot`, сделать конфигурируемым.

**Выполнено:**
- Добавлена переменная `TELEGRAM_BOT_USERNAME=PartyQuizBot` в корневой `.env`
- Обновлён `docker-compose.yml`:
  - Добавлен `env_file: - .env` для `wedding_web`
  - Добавлен маппинг `VITE_TELEGRAM_BOT_USERNAME: ${TELEGRAM_BOT_USERNAME}`
- `TVLobby.jsx` использует `import.meta.env.VITE_TELEGRAM_BOT_USERNAME`
- Создан `.env.example` в корне для документации

**Файлы:**
- `.env` — добавлена переменная
- `docker-compose.yml` — маппинг для web service
- `apps/web/src/components/TV/TVLobby.jsx`
- `.env.example`

---

### ✅ Дополнительно: Улучшение seed (две кнопки)
**Цель:** Добавить безопасный seed (не удаляет данные) + полный сброс.

**Выполнено:**
- **Backend:**
  - Создана функция `runSeed()` — добавляет демо-квиз БЕЗ удаления
  - Создана функция `runFullReset()` — удаляет ВСЁ и создаёт демо
  - Функция `createDemoQuiz()` — общая логика создания демо-квиза
- **API:**
  - `POST /api/admin/seed` → `runSeed()` — безопасный
  - `POST /api/admin/reset` → `runFullReset()` — деструктивный
- **Client:**
  - `adminApi.reseed()` — безопасный
  - `adminApi.fullReset()` — полный сброс
- **UI (Home.jsx):**
  - Две кнопки: "➕ Добавить демо-квиз" (зелёная), "⚠️ Полный сброс БД" (красная)
  - Разные confirm диалоги для каждой операции

**Файлы:**
- `apps/api/src/services/seed-service.ts`
- `apps/api/src/routes/admin.ts`
- `apps/web/src/api/client.js`
- `apps/web/src/pages/Home.jsx`

---

### ✅ Дополнительно: Выбор квиза для показа на ТВ
**Цель:** Возможность выбрать какой draft-квиз показывать на ТВ.

**Проблема:** При наличии 2+ draft-квизов невозможно выбрать какой показывать на ТВ.

**Решение:**
- **База данных:**
  - Добавлено поле `displayedOnTv boolean NOT NULL DEFAULT false` в таблицу `quizzes`
  - Миграция: `0003_gray_lily_hollister.sql`
- **API:**
  - Endpoint `POST /api/quizzes/:id/display-on-tv`
  - При установке флага у одного квиза, сбрасывает у всех остальных
- **Client:**
  - `quizzesApi.displayOnTv(id)`
- **Layout.jsx:**
  - `refreshTvCode()` приоритизирует квиз с `displayedOnTv = true`
  - Фоллбек: первый неархивированный квиз
- **Home.jsx:**
  - Добавлен state `displaying`
  - Добавлен handler `handleDisplayOnTv()`
  - **Новая логика кнопок для draft-квизов:**
    - Если `displayedOnTv = false`: показывает "📺 Вывести на экран" (синяя кнопка)
    - Если `displayedOnTv = true`: показывает "▶️ Начать" (оранжевая кнопка)
  - Реализовано как для мобильной, так и для десктопной версии

**Workflow:**
1. Создайте 2+ квиза в статусе "Черновик"
2. Нажмите "📺 Вывести на экран" на нужном квизе
3. Квиз появится на ТВ (ссылка в хедере обновится)
4. Кнопка изменится на "▶️ Начать"
5. Нажмите "Начать" чтобы запустить игру

**Файлы:**
- `apps/api/src/db/schema.ts` — новое поле
- `apps/api/drizzle/0003_gray_lily_hollister.sql` — миграция
- `apps/api/src/routes/quizzes.ts` — новый endpoint
- `apps/web/src/api/client.js` — новая функция
- `apps/web/src/components/Layout.jsx` — обновлён `refreshTvCode()`
- `apps/web/src/pages/Home.jsx` — новый handler и UI

---

## Обновлённая документация

### Файлы документации обновлены:
- `PLAN.md` — отмечены этапы 1-3 как выполненные, добавлены дополнительные улучшения
- `CLAUDE.md` — обновлён список реализованных функций
- `docs/API.md` — добавлены новые endpoints:
  - `POST /quizzes/:id/display-on-tv`
  - `POST /admin/seed` (обновлено описание)
  - `POST /admin/reset` (новый)
- `docs/DATABASE.md` — добавлено поле `displayedOnTv` в таблицу `quizzes`
- `DONE.md` — создан этот файл с историей выполненных задач

---

## Итого за сессию

**Выполнено:**
- ✅ Этап 1: Удаление OCR
- ✅ Этап 2: Улучшение экрана регистрации
- ✅ Этап 3: Улучшение экрана результатов
- ✅ Конфигурация bot username через .env
- ✅ Две кнопки seed (безопасная + полный сброс)
- ✅ Выбор квиза для показа на ТВ

---

## 2026-02-25 (продолжение): Тестовые агенты (боты)

### ✅ Реализация системы тестовых ботов
**Цель:** Автоматизация тестирования квизов без реальных участников.

**Проблема:** Тестирование квиза требует набирать команду людей каждый раз, что долго и неудобно.

**Решение:**
- Изолированный модуль тестовых ботов (легко удалить)
- Боты автоматически регистрируются и отвечают случайно через 1 секунду
- Галочка "Показывать на TV" для скрытия ботов от зрителей
- Автоудаление при завершении квиза

**Выполнено:**

#### 1. База данных
- Добавлено поле `is_bot` в таблицу `teams`
- Добавлено поле `show_bots_on_tv` в таблицу `game_state`
- Миграция: `0004_dazzling_prima.sql`

#### 2. Backend: изолированный модуль `test-agents/`
**Структура:**
```
apps/api/src/test-agents/
├── index.ts                    # публичный API модуля
├── types.ts                    # Bot, BotConfig
├── bot-answer.generator.ts     # генерация случайных ответов
└── bot-agent.service.ts        # управление ботами
```

**Логика генерации ответов:**
- Multiple choice: случайный выбор A/B/C/D
- Text answer: 50% правильного ответа (первая половина строки)

**API endpoints** (`routes/test-agents.ts`):
- `POST /api/quizzes/:id/test-bots` — создать N ботов (1-20)
- `DELETE /api/quizzes/:id/test-bots` — удалить всех ботов
- `POST /api/game/:id/toggle-bots-visibility` — показ на TV

#### 3. Интеграция в основной код
**`index.ts`:**
- Создание `BotAgentService` с WebSocket
- Регистрация `testAgentsRoutes`
- Экспорт `getBotService()` для доступа из других модулей

**`game-service.ts`:**
- При `setSlide("timer")` → боты отвечают через 1 сек
- При `finishGame()` → автоудаление всех ботов

**`teams.ts`:**
- Фильтрация ботов из списка команд если `showBotsOnTv = false`

#### 4. Frontend: панель управления ботами
**Компонент** `TestBotsPanel.jsx`:
- Поле ввода количества ботов (1-20)
- Кнопка "➕ Добавить" (disabled если боты уже есть)
- Отображение количества активных ботов
- Галочка "Показывать на TV" (по умолчанию включена)
- Кнопка "🗑️ Удалить всех ботов"
- Подсказка: "Боты отвечают случайно через 1 сек. Удаляются автоматически при завершении квиза."

**Интеграция** в `Game.jsx`:
- Панель отображается на экране регистрации
- Обновление списка команд после добавления/удаления

#### 5. Workflow использования
1. Создать квиз и открыть регистрацию
2. В панели "🤖 Тест-боты" ввести количество (например, 5)
3. Нажать "➕ Добавить"
4. Боты регистрируются мгновенно: `🤖 Бот 1`, `🤖 Бот 2`, ...
5. Начать квиз
6. При показе вопроса → переключить на слайд "Таймер"
7. Боты автоматически отвечают через 1 секунду
8. Проверить статистику ответов в реальном времени
9. При завершении квиза → боты автоматически удаляются

#### 6. Документация
**PLAN.md:**
- Добавлена полная инструкция по удалению модуля (перед "## Опционально в будущем")
- Пошаговые команды для удаления файлов, импортов, интеграций
- SQL команды для удаления полей из БД (опционально)

**API.md:**
- Добавлен раздел "Тестовые агенты (боты)" с 3 endpoints

**DATABASE.md:**
- Добавлено поле `isBot` в таблицу `teams`
- Добавлено поле `showBotsOnTv` в таблицу `game_state`

**Файлы:**
- `apps/api/src/test-agents/` (4 файла)
- `apps/api/src/routes/test-agents.ts`
- `apps/api/src/index.ts` (интеграция)
- `apps/api/src/services/game-service.ts` (2 хука)
- `apps/api/src/routes/teams.ts` (фильтрация)
- `apps/web/src/components/Admin/TestBotsPanel.jsx`
- `apps/web/src/pages/Game.jsx` (интеграция)
- `apps/api/drizzle/0004_dazzling_prima.sql` (миграция)

---

### ✅ Исправление: WebSocket события от ботов
**Дата:** 2026-02-25 (поздний вечер)

**Проблема:**
- Боты отвечали на вопросы и записывали ответы в БД
- API логи показывали что боты работают
- НО: админка не получала WebSocket события `answer_submitted`
- Админ-панель не видела что боты "сдали" ответы
- Невозможно было вести квиз с ботами (не видно кто ответил)

**Причина:**
Неправильный вызов функции `broadcast()` в `bot-agent.service.ts`:

```typescript
// ❌ Неправильно (1 объект):
this.wsServer.broadcast({
  event: "answer_submitted",
  data: { ... }
});

// ✅ Правильно (2 аргумента):
this.wsServer.broadcast("answer_submitted", { ... });
```

Функция `broadcast(event: string, data: unknown)` ожидает 2 аргумента, а боты передавали 1 объект.

**Исправление:**
- Изменён вызов в `bot-agent.service.ts:128`
- Теперь соответствует сигнатуре функции и другим вызовам в проекте

**Результат:**
- WebSocket события от ботов теперь доходят до админки
- Админ-панель показывает что боты ответили (зелёные галочки)
- Можно полноценно вести квиз с тестовыми ботами

**Файлы:**
- `apps/api/src/test-agents/bot-agent.service.ts:128` — исправлен вызов broadcast

---

**Следующие этапы** (см. PLAN.md):
- Этап 4: Web TV + Admin — места появляются при клике из админки
- Этап 5: Telegram Bot — капитаны получают вопрос только на слайде таймера
- Этап 6: API — улучшить оценку текстовых ответов
- Этап 7: Слайды "Спасибо" в конце квиза
- Этап 8: Дополнительные слайды между вопросами (шутки, mp4)
- Этап 9: Улучшить seed Demo quiz
